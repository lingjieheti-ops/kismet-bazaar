{
  "name": "kismet-agents",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "KISMET agent syndicates: data reporters, underwriter pricing, keeper decisions",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "decide": "tsc && node dist/keeper/decide.js",
    "demo": "tsc && node dist/demo/floor.js"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
